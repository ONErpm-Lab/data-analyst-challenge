analytics_dbt:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', 'data/database/onerpm_analytics.duckdb') }}"
      threads: 4
      schema: main
      settings:
        preserve_insertion_order: false